{
  "name": "Student Support Chatbot",
  "install": "./.cursor/scripts/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd \"$(git rev-parse --show-toplevel)\" && export PATH=\"${HOME}/.local/bin:${PATH}\" && set -a && [ -f .env ] && source .env; set +a && uv run uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload"
    },
    {
      "name": "frontend",
      "command": "cd \"$(git rev-parse --show-toplevel)/frontend\" && npm run dev -- --host 127.0.0.1 --port 5173"
    }
  ],
  "ports": [
    {
      "name": "backend-api",
      "port": 8000
    },
    {
      "name": "frontend-dev",
      "port": 5173
    }
  ]
}
