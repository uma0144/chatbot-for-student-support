# Optional — API URL for production builds or direct backend calls.
# Local dev: leave unset so Vite proxies /api to http://127.0.0.1:8080 (works on any port 5173+).
# If set, use your backend port (e.g. 8080 on Windows):
# VITE_API_BASE_URL=http://127.0.0.1:8080
